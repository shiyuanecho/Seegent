# 技能库浏览器（Skill Library）— 给开发 AI 的执行指令

> 你是全栈开发 AI。请 Read 本文件后直接开工，不要追问用户（用户是技术小白，无法回答实现细节问题）。
> 配套设计文档：`技能库浏览器-实现方案.md`（同目录，必读）。
> 主定位文档：`Seegent-PRD.md`（同目录，了解项目定位用）。

---

## 第 0 步：先读这些文件（顺序执行，不要跳过）

1. `技能库浏览器-实现方案.md` ← 需求、数据格式、验收标准全在这
2. `Seegent-PRD.md` ← 了解 Seegent 是"本地项目驾驶舱"的定位，确认技能库是并列的独立模块
3. `seegent/static/index.html` ← 前端单文件（约 536KB）。重点定位：
   - `switchModule()` 函数：现有模块（file/chat/board/...）怎么注册和切换
   - 侧边栏导航 HTML：新页签加在哪
   - `bindFolderToBoard()` 相关函数：复用它的"选文件夹/绑定"交互与存储方式
   - 现有 Markdown 渲染函数：详情区渲染 SKILL.md 要复用它
   - 现有搜索/过滤组件：卡片墙搜索复用
   - ⚠️ 行号会漂移，用 grep 按函数名/注释定位，别死守行号
4. `seegent/server.py` ← 后端。确认现有"读本地目录/读文件"的能力（看板绑定已经能读用户选的文件夹），决定扫描 SKILL.md 是走后端 API 还是前端 File System Access API

**关键决策（由你判断，但默认走后端）**：
- 读取 `~/.workbuddy/skills` 这种**任意路径**的本地文件夹，前端 File System Access API 刷新后授权会失效、持久化麻烦。
- **建议新增后端 API**（`POST /api/skills/scan` 等，详见实现方案第 7.2 节），更稳、刷新不丢。除非现有架构明显适合纯前端，否则优先后端。

---

## 第 1 步：备份（铁律，必须做）

修改 `seegent/static/index.html` 前，**先备份**：

```bash
cp seegent/static/index.html "seegent/static/index.html.backup-$(date +%Y%m%d)"
```

若改动 `seegent/server.py`，同样备份。备份成功再动手。

---

## 第 2 步：MVP 范围（第一版只做这些，做完验收通过再谈增强）

### ✅ 必须做（P0）

1. **新增独立页签「🧩 技能库」**
   - 与现有 file / chat / board 模块并列，注册进 `switchModule()` 和侧边栏
   - 不改动任何现有模块（file/chat/board/模型/角色/MCP）的逻辑

2. **添加技能文件夹（支持多个）**
   - 页签内「➕ 添加文件夹」按钮，复用现有文件夹选择/绑定交互
   - 用户可手动添加多个文件夹（如 `~/.workbuddy/skills`、`~/拾元/skills`）
   - 已添加的来源列表可管理，可删除某个来源
   - 「重新扫描」按钮：刷新索引

3. **递归扫描 + 解析**
   - 对每个来源文件夹递归找所有 `SKILL.md`（含 SKILL.md 的父目录 = 1 个 Skill）
   - 解析 YAML frontmatter（`name`/`description`/`version`/`category`/`platforms`/`slug`/`displayName`，**全部可选，缺失要容错**）
   - 生成索引 `.seegent-skill-index.json`（结构见实现方案第 6 节）
   - 持久化：刷新页面后索引仍在

4. **结构化浏览（三区布局）**
   - 左栏：分组（按 category / 来源目录 / 字母）+ 顶部搜索框（匹配 name + description，实时过滤）
   - 中栏：卡片墙，每张卡显示——展示名（displayName 优先，否则 name）、description 摘要（**截断 ~120 字**，详情才看全文）、category/platforms 标签、是否多文件标记、来源文件夹名
   - 右栏：点卡片展开 → Markdown 渲染该 SKILL.md **全文**（复用现有渲染）+ 元信息（name/version/category/platforms/slug）

5. **空状态 / 错误态**
   - 未添加文件夹：引导「➕ 添加你的 Skills 文件夹」
   - 文件夹无 SKILL.md：提示「该文件夹未发现 Skill」
   - frontmatter 解析失败：Skill 仍展示，标「元数据解析失败」，至少显示目录名

### ❌ 第一版不做（留后续，不要做）

- ❌ 文件树子文件查看（config/references/scripts 里的文件点开看内容）——先只渲染 SKILL.md 全文
- ❌ 关系视图 / 关系图（category 分组着色可顺手做，力导向图不做）
- ❌ 「用 AI 解释这个 Skill」按钮
- ❌ 编辑/创建/删除 Skill 文件、执行 Skill、Skill 市场

> 范围控制原因：Seegent 前端是 536KB 单文件，改动易出 Bug；用户最烦重复 Bug。第一版把"能加文件夹 + 能看卡片 + 能点开看全文"跑通最稳。

---

## 第 3 步：验收标准（MVP 逐条对照，全过再交付）

- [ ] 侧边栏出现「🧩 技能库」页签，点击进入独立页面
- [ ] 「➕ 添加文件夹」可选本地目录，添加成功
- [ ] 可添加多个文件夹，可删除某个来源
- [ ] 添加后卡片墙自动出现该文件夹下所有 Skill（每个含 SKILL.md 的目录 = 1 张卡）
- [ ] 每张卡显示展示名、description 摘要、category/platforms 标签、多文件标记、来源
- [ ] 点卡片，右栏 Markdown 渲染该 SKILL.md 全文 + 元信息
- [ ] 搜索框按 name + description 实时过滤卡片
- [ ] 左栏可按 category / 来源 / 字母分组
- [ ] 空状态（无文件夹 / 无 SKILL.md / 解析失败）均有明确提示
- [ ] 刷新页面后技能库索引仍在（持久化）
- [ ] 现有功能（文件管理、AI 对话、看板、模型/角色/MCP）完全不受影响

---

## 第 4 步：约束（务必遵守）

- **隐私优先**：所有解析在本地完成，Skill 内容不上传任何第三方
- **容错**：frontmatter 字段缺失 / 格式不规范（含 `key: |` 多行块）不能崩，至少显示目录名
- **复用优先**：文件夹选择、Markdown 渲染、搜索、文件读取，全部复用现有实现，别重复造轮子
- **不破坏现有架构**：新增独立模块，不动 file/chat/board 等现有逻辑
- **description 可能极长**（有的近 300 字）：卡片摘要必须截断，详情区才显示全文

---

## 第 5 步：交付

完成后，简短说明：
1. 改了哪几个文件
2. 新增了哪些 API（若有）
3. 第一版做了什么、留了什么到后续
4. 有没有遇到实现方案没覆盖的坑（若有，说明怎么处理的）

不要替用户做"关系视图""文件树子文件"等后续功能，除非用户另行要求。
