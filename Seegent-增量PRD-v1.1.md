# Seegent 增量 PRD v1.1 —— 首个开发迭代

> 文档性质：开发增量 PRD（首个迭代）
> 日期：2026-07-08
> 作者：产品经理 许清楚（Xu）
> 输入依据：主 PRD v1.0、Seegent-定位与功能梳理.md（2026-07-08 结论）、看板升级-首页改造-实现方案.md、架构师代码核查事实、用户拍板决策
> 范围：路线图 now→1 月，交付两个 P0（清理残留 + 看板首页）
> 约束：本文档仅描述需求，不写代码、不改文件；具体文件行号均来自已确认代码核查

---

## 1. 目标（一句话）

本迭代交付一个**方向自洽、开箱即用的项目管理指挥部**：先移除 v0.1 多智能体/Shell 残留、收敛为单一管理型 Agent，再把**看板设为默认首页**，使独立开发者打开 Seegent 第一眼即见所有项目状态、待办与最近活动。

---

## 2. P0 前置 —— 清理 v0.1 多智能体残留

### 2.1 背景（来源：架构师高见远代码核查）

文档（v0.1 多智能体平台 PRD）已归档，但其代码**未归档、仍活跃于生产**，与 v1.0「指挥部」定位直接冲突，导致方向在代码层「说一套做一套」。已确认活跃残留如下：

| 残留项 | 代码位置 | 当前状态 |
|--------|----------|----------|
| `invoke_agent` 工具定义 | `server.py:4795` | 活动 |
| `invoke_agent` 执行体（嵌套编排） | `server.py:5140–5392` | 活动 |
| `run_shell` 工具（default-general 白名单含它） | `server.py:4824` | 活动 |
| Agent 注册表 | `.seegent-agents.json`（5 个 active）/ `GET /api/agents` | 活动 |
| 前端多 Agent SSE 渲染 | `index.html:12341–12441` | 活动 |

结论：必须在「看板首页」等增量功能落地前完成清理，否则指挥部定位名不副实。

### 2.2 用户故事

> 作为用户，我希望 Seegent 不再暴露多智能体 / Shell 能力，只保留单一管理型 Agent，使「指挥部」定位名副其实，避免误用和认知混乱。

### 2.3 验收标准（可勾选）

- [ ] 仓库内无 `invoke_agent` 可调用入口（定义 `server.py:4795` 与执行体 `server.py:5140–5392` 已移除）
- [ ] 仓库内无 `run_shell` 可调用入口（含从 default-general 工具白名单移除 `server.py:4824`）
- [ ] Agent 注册表概念已删除：`.seegent-agents.json` 移除或置空；`GET /api/agents` 禁用或移除
- [ ] 前端无多 Agent 编排 UI（`index.html:12341–12441` 多 Agent SSE 渲染相关代码已移除）
- [ ] 系统仅存**单一管理型 Agent**（内置 AI 仅做「管项目」类工作：分析 / 总结 / 理待办 / 周报，不写代码、不做图、不跑自动化）
- [ ] 现有指挥部功能不受影响：看板 / 文件 / AI 对话 / 搜索 / 数据源 / 角色 / MCP 模块仍可正常使用

### 2.4 注意（收敛边界）

- **保留**：角色系统 `.seegent-roles.json`（仅删「多 Agent 注册表」概念，角色不属此列）
- **保留但裁剪**：AI 对话的 `_agent_loop` 保留运行；仅移除 `invoke_agent` / `run_shell` 两个工具，不破坏对话主链路
- **不改动**：C 连接器（本期不做，仅作长期备注，接口保持开放，可接 Claude Code / Codex / WorkBuddy 等任意外部执行器）

---

## 3. P0 —— 看板首页升级

### 3.1 背景

当前默认进入「文件管理」模块（`index.html` 中 `let activeModule = localStorage.getItem('seegent-module') || 'file';`，约 `index.html:6920`）。看板只是侧边栏中的一个 tab。用户希望打开即见项目总览，落实「指挥部每天第一站」。

### 3.2 用户故事

> 作为独立开发者，我希望打开 Seegent 第一眼看到所有项目状态 + 待办 + 最近活动，落实「指挥部每天第一站」，无需任何配置即可开工。

### 3.3 验收标准（可勾选）

- [ ] 首页（看板模块）默认展示，**开箱即用无需配置**（首次打开直接进入看板，不再默认文件模块）
- [ ] 前端 `activeModule` 默认值由 `'file'` 改为 `'board'`（`index.html` 约 `:6920`），并兼容老用户已存 `localStorage` 模块记忆
- [ ] 首页聚合**项目卡片**：复用看板 V1 接口 `GET /api/board/projects` + 飞书多维表格数据源（`POST /api/dashboard/sync-feishu`），卡片含：项目名/图标、核心指标（仅显示有数据的）、待办数徽章、最后活动时间、操作区（进入项目/查看文件）
- [ ] 首页含**各项目待办摘要区**：汇总所有项目待办（`/api/board/projects` 已返回每个项目 `todos`），显示勾选框 + 待办文字 + 所属项目标签 + 日期；支持勾选完成（前端状态切换，本期不持久化）
- [ ] 首页含**最近活动流区**：取 `fileTree` 中 mtime 最新的 5 条文件变更，显示时间 + 文件类型图标 + 文件名 + 所属项目标签
- [ ] 无数据时空状态友好：无项目绑定 / 无待办 / 无活动时分别给出引导文案
- [ ] 与现有文件 / AI 对话模块可正常切换，互不破坏
- [ ] 现有指挥部功能（看板数据源、文件、AI 对话、搜索、角色、MCP）不受影响

### 3.4 参考实现方案（佐证，以本 PRD 为准）

依据 `看板升级-首页改造-实现方案.md`：

- **改动范围**：主要改前端 `static/index.html`；后端 `server.py` **不新建 API、不改接口**，复用现有 `/api/board/projects` 与 `/api/dashboard/*`
- **关键代码位**：看板 HTML 模板 `index.html:~2904`（#board-module）、看板 CSS `~2016`、默认模块 `:6920`、看板 JS 初始化 `initDashboard() :6963`、数据加载 `loadBoardProjects() :~9109`、渲染 `renderBoardProjects() :~9128`
- **不做**：不新建后端 API、不改设置模块、不实现新数据源类型、不实现待办持久化、不改文件/聊天模块、不做项目工作流追踪
- **API 数据结构**：`GET /api/board/projects` 已返回每项目 `todos[]` 与 `fileTree`，可直接渲染待办区与最近活动区
- **样式约束**：沿用现有 CSS 变量体系（`--bg-primary` 等）与 `.dash-project-card` 卡片类；模块数量保持 5 个不变

---

## 4. 需求池

### P0 前置（本迭代必做）
| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| R0-1 | 移除 `invoke_agent` 定义与执行体 | P0 | `server.py:4795`、`server.py:5140–5392` |
| R0-2 | 移除 `run_shell` 及其白名单条目 | P0 | `server.py:4824`（default-general） |
| R0-3 | 删除 Agent 注册表 | P0 | `.seegent-agents.json` 移除/置空；`/api/agents` 禁用或移除 |
| R0-4 | 移除前端多 Agent SSE 渲染 | P0 | `index.html:12341–12441` |
| R0-5 | 收敛为单一管理型 Agent | P0 | 保留 `_agent_loop`，仅去两个工具；保留角色系统 |

### P0 看板（本迭代必做）
| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| R1-1 | 看板设为默认首页 | P0 | `activeModule` 默认 `'board'`（`index.html:6920`），兼容 localStorage |
| R1-2 | 项目卡片增强 | P0 | 指标 / 待办徽章 / 最后活动 / 操作区 |
| R1-3 | 待办汇总区 | P0 | 复用 `todos`，前端勾选不持久化 |
| R1-4 | 最近活动流区 | P0 | 取 `fileTree` 最新 5 条 |
| R1-5 | 空状态与引导 | P0 | 无项目/无待办/无活动各提示 |

### 后续路线图（仅参照，不展开）
- **P1 待办增强**：自动扫描 TODO 聚合 + 完成态持久化（打通看板首页）
- **P1 AI 项目分析**：基于项目数据/活动生成趋势与周报（AI 仅分析总结，不写代码不做图）
- **P2 角色精简**：角色 12→3-5，降低小白认知负荷
- **P2 设置整合**：分散设置收拢统一入口，API Key 自管/隐私设置可见明确

---

## 5. 明确不做（结合已确认定位 A+C，拒 B）

| 不做项 | 理由 |
|--------|------|
| **B 多智能体平台**（Agent 注册 / DAG / 编排） | 主 PRD v1.0 已定位指挥部；多 Agent 编排对技术小白成本高，与「多项目一眼看清」刚需不匹配；本期已做清理前置 |
| **C 连接器（本期）** | 用户已拍板本期先不做；列为长期开放方向，接口保持开放（可接 Claude Code / Codex / WorkBuddy 等任意外部执行器） |
| **Shell / 终端** | 主 PRD 非目标；本期清理已含删 `run_shell` |
| **Agent 市场 / 社区** | 主 PRD 非目标；收敛为单一管理 Agent |
| **自动路由 / 意图识别** | 主 PRD 非目标；指挥部以人主导，不自动派活 |
| **社交媒体数据监测** | 主 PRD 非目标；用户已有独立数据监测网站 |

---

## 6. 待确认 / 风险

1. **清理残留是否需保留一键回滚？**
   - 状态：**待开发前与用户确认**
   - 说明：移除 `invoke_agent` / `run_shell` / Agent 注册表属不可逆删改。若用户希望保留回滚通道，需明确以 git tag 或分支方式留痕，而非在生产保留开关。

2. **看板首页默认态切换是否影响老用户习惯？**
   - 状态：**低风险默认处理**
   - 说明：`index.html:6920` 已用 `localStorage.getItem('seegent-module')` 记忆上次模块，老用户仍进入其上次模块；仅对**首次/无记忆**用户默认看板。无需额外确认，按既有逻辑实现即可。

---

> 说明：本 PRD 为需求基线，不触及实现。下游架构师据此做架构设计与任务分解，工程师据此实现，QA 据此验收。C 连接器与多智能体平台不在本迭代，仅作长期备注。
