# Seegent 能力库（角色 + 技能）PRD

> ⚠️ 已被 **Seegent-能力模块-PRD.md（v0.2）** 取代。旧版"一个能力库含两个 tab"结构，已按 v0.2 拆分为「助手」「技能」两个独立模块，并新增「扩展」占位与「配置仅模型」决定，连接器划除。本文件保留仅作历史参考。

> 版本：v0.1 草案 | 日期：2026-07-14
> 性质：新模块方案，承接 2026-07-08 定位（A+C 指挥部，拒绝 B 多智能体）
> 关联文档：Seegent-PRD.md、Seegent-定位与功能梳理.md、看板升级-首页改造-实现方案.md

---

## 1. 背景与动机（Why）

用户是同时运营多个项目（识字 App、Learn Chinese、小红书等）的独立开发者。日常的真实瓶颈不是"没有 AI 帮忙"，而是：

- 角色和技能散落在各个平台的对话里，想复用时找不到、复制不全；
- 已有的 12 个内置角色是死配置，和真实工作流脱节，几乎不用；
- 每次换平台（Claude Code / WorkBuddy / Cursor / ChatGPT 网页）都要重写一遍"你是谁、用什么方法"；
- 想要的是一个**随时可调用的"我的能力库"**：根据当下内容需求，随手召唤对应角色 + 对应技能，到任何正在用的智能体里。

一句话：**在 AI-native 工作流里，角色和技能是用户自己的可移植资产。Seegent 负责把它们定义好、管理好、随时导出去用；执行永远在别的平台。**

---

## 2. 产品定位

```
Seegent 能力库 = 你的角色中台 + 技能中台（合称"能力中台"）

  Seegent（管理 + 呈现 + 导出）
  ┌─────────────────────────────────────┐
  │  团队/   → 角色文档（你是谁）          │
  │  技能/   → 技能文档（用什么方法）       │
  │  能力库页面：分组呈现 + 一键召唤        │
  └─────────────────────────────────────┘
            │ 复制召唤指令（纯文本）
            ▼
  任意执行平台：Claude Code / WorkBuddy / Cursor / ChatGPT 网页 …
            │ 真正干活
            ▼
        产出落回你的文件夹
```

边界：Seegent **不执行、不编排、不调用任何外部 Agent**。它只管"能力资产"的定义、呈现、导出。这严格延续 2026-07-08 的 A+C 定位（A 指挥部 + C 未来轻连接），不滑回 B 多智能体。

---

## 3. 目标用户

1. 独立开发者（用户本人）：多项目，需要在不同平台间复用同一套角色 / 技能。
2. 未来：任何想把"自己的 AI 工作流"沉淀成可复用资产的人。

---

## 4. 核心概念：角色 vs 技能，以及如何组合

这是整个设计的关键区分：

| 维度 | 角色（Role） | 技能（Skill） |
|------|-------------|--------------|
| 回答的问题 | 以什么身份 / 视角来做 | 用什么方法 / 流程来做 |
| 例子 | 数据解读专家、内容主编、项目经理 | 小红书爆款标题、竞品分析框架、周报生成 |
| 文件位置 | `团队/` 文件夹 | `技能/` 文件夹 |
| 在召唤里的作用 | 定"人设 / 口吻 / 专业立场" | 定"操作步骤 / 模板 / 约束" |

组合召唤（用户描述的"画面感"）：

```
【角色】请以"数据解读专家"的身份（设定见 ./团队/数据组/小辉.md）
【技能】按"趋势解读"的方法执行（见 ./技能/数据组/趋势解读.md）
【任务】这是我这周的小红书数据：<粘贴数据>
请结合以上角色与技能，给出趋势判断和下周建议。
```

角色给"立场"，技能给"方法"，任务是"具体内容"。三者可拆可合——这正是 AI-native 可组合能力的核心。

---

## 5. 功能范围

### 5.1 文件夹约定

在 Seegent 绑定的根目录下，约定两个专属文件夹：

```
<你的根目录>/
├── 团队/                ← 角色库（组织架构）
│   ├── 数据组/
│   │   ├── 小辉.md       （角色：AI/代码知识库）
│   │   └── 小禾.md
│   ├── 内容组/
│   │   └── 小蓝.md
│   └── 管理组/
│       └── 小琪.md
└── 技能/                ← 技能库
    ├── 内容组/
    │   └── 小红书爆款标题.md
    └── 数据组/
        └── 趋势解读.md
```

- 子文件夹 = 分组（采用方案 A）。
- 每个 `.md` = 一个角色或一个技能。
- 识别方式：扫描到 `团队/` 或 `技能/` 文件夹即视为能力库，自动在能力库页面呈现。（备选：用根目录标记文件 `.seegent-capabilities` 显式声明，二选一，推荐前者更"零配置"。）

### 5.2 文档格式

角色文档 `团队/.../小辉.md`：

```markdown
---
seegent-type: role
name: 小辉
title: AI/代码知识库
group: 数据组
emoji: 📚
datasources: [小红书运营数据]
---

（角色的职责、专业立场、输出风格、注意事项……）
```

技能文档 `技能/.../趋势解读.md`：

```markdown
---
seegent-type: skill
name: 趋势解读
group: 数据组
emoji: 📈
inputs: [时序数据]
outputs: [趋势判断, 建议]
---

（技能的操作步骤、模板、约束、示例……）
```

`seegent-type` 区分角色 / 技能；`group` 用于分组；正文即召唤时粘贴的内容。

### 5.3 自动识别

- 添加文件夹时，Seegent 递归扫描是否含 `团队/` 或 `技能/`。
- 命中即纳入能力库，无需手动配置。
- 新增 / 改名 / 删除 `.md` 文件，刷新即同步（单一事实源，无额外存储）。

### 5.4 能力库页面（呈现，照用户提供的组织架构图样式）

- 一个独立模块"能力库"，内含 **角色 / 技能** 两个视图（tab）。
- 每个视图：左侧分组列表（数据组 / 内容组 / 管理组…）→ 右侧该组成员。
- 每个成员一行：图标 + 名字 + 职责一句话 + 操作（查看文档 / 召唤）。
- 点成员 → 右侧面板显示该文档完整正文（渲染 markdown）。

### 5.5 召唤机制（核心交付）

每个角色 / 技能详情处一个"召唤"按钮，选目标平台生成指令：

| 目标 | 生成内容 | 用法 |
|------|---------|------|
| 本地智能体（Claude Code / WorkBuddy / Cursor，能读该文件夹） | `请读取 <相对路径> 并按其设定扮演/执行，帮我：<任务>` | 复制 → 粘贴 |
| 通用平台（ChatGPT 网页等，读不到本地路径） | 把文档正文整段 + "请按以上设定…" | 复制 → 粘贴 |

组合召唤：在能力库可分别点"复制角色指令"和"复制技能指令"，拼到一起发给目标平台（见 §4 示例）。

- 召唤是**纯文本**，不依赖 API / 连接器 / 任何技术打通。今天即可用。
- 深度"一键派单自动跑"属 PRD 的 C 连接器长期方向，本期不做。

### 5.6 文档查看与编辑

- 查看：能力库内直接渲染 `.md` 正文。
- 编辑：点"编辑"跳转到现有文件模块打开该 `.md`，不在能力库内另造编辑器。

### 5.7 移除内置 12 角色

- 第一步即清理 `.seegent-roles.json` 的 12 个内置角色（先备份）。
- 聊天模块的角色选择改为从能力库（团队文件夹）读取；不选手动角色 = 基础模型。
- 现有"技能库浏览器"若与新建技能库定位冲突，后续评估合并 / 取舍（见风险）。

---

## 6. 非目标（Non-goals）

- 不在 Seegent 内执行角色或技能（不写代码 / 做图 / 跑自动化）。
- 不做多 Agent 编排、DAG、Agent 注册表（B 路线，已否）。
- 不做 API / 连接器级别的"一键派单"（C 长期方向）。
- 不在能力库内做富文本编辑器。
- 不内置任何预置角色 / 技能（全部用户自己定义）。

---

## 7. 成功指标

| 指标 | 定义 | 目标（基线待定） |
|------|------|----------------|
| 能力库采用率 | 每周从能力库发起"召唤"的次数 | > 0，逐步上升 |
| 角色 / 技能复用率 | 单个角色 / 技能被召唤的频次 | 核心角色周复用 ≥ 3 次 |
| 内置角色下线后零回退 | 聊天是否仍依赖旧内置角色 | 0 依赖 |
| 管理效率 | 新增一个角色 / 技能的平均耗时 | < 5 分钟（写一个 .md） |

---

## 8. 路线图

- **Now（P0）**：文件夹约定 + 文档格式 + 自动识别 + 能力库页面（角色视图）+ 召唤按钮 + 移除内置 12 角色。
- **Next（P1）**：技能视图 + 组合召唤辅助（一键拼角色 + 技能）。
- **Later（P2）**：与看板首页打通（在首页显示"我的团队 / 能力"概览）；评估与现有技能库浏览器合并。

---

## 9. 风险与开放问题

- **资产维护**：库的价值取决于文档质量。需引导用户写好 frontmatter + 正文（可给模板）。
- **与现有技能库浏览器的关系**：项目已有 `技能库浏览器`，定位可能重叠，需明确分工或合并。
- **识别误判**：若用户文件夹恰有同名 `团队/技能` 目录，可能误识别。用标记文件 `.seegent-capabilities` 可兜底。
- **多项目**：每个项目根目录可有自己的 团队/技能，还是全局一套？建议先支持每项目各自一套，后续可加"全局共享库"。

---

*草案状态：待用户 review。下一步据此细化实现方案或拆任务。*
