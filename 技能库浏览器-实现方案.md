# 技能库浏览器（Skill Library）— 实现方案

> 给执行 AI 的直接实现文档。Read 这份文件即可开工，无需追问。
> 配套主文档：`Seegent-PRD.md`（定位：项目管理指挥部）

---

## 1. 背景与 Why

用户（独立开发者）在本地多个文件夹里维护了大量 Skill（主要位于 `~/.workbuddy/skills/`，也可能包括 `~/拾元/skills`、`/.claude/skills` 等）。每个 Skill 是一个子目录，内含 `SKILL.md`（YAML frontmatter + Markdown 正文），多文件 Skill 还有 `config/`、`references/`、`scripts/` 等子目录。

**痛点**：
- 记不住每个 Skill 具体干什么（description 很长，平铺文件看不过来）
- 不知道 Skill 之间的关系
- 想用的时候找不到

**目标**：Seegent 新增一个独立的「🧩 技能库」页签。用户手动添加任意多个文件夹 → 自动递归扫描其中所有 `SKILL.md` → 结构化展示每个 Skill 的用途与内容 → 支持查看完整文件 → 最好能展示 Skill 之间的关系。

**为什么放在 Seegent**：这是 Seegent「本地文件 + AI 语义理解」护城河的直接体现——直接读真实文件、自动解析，而不是像 Notion 那样手动录入。

---

## 2. 目标与用户故事

> **作为** 管理大量本地 Skill 的用户，**我希望** 在 Seegent 里添加 Skill 文件夹后能一眼看到所有 Skill 的用途与内容，**以便** 快速找到对的工具、理解它们的关系，对资产了如指掌。

验收对象：单个用户、本地优先、纯浏览（不编辑、不执行）。

---

## 3. 数据源格式（已实测）

每个 Skill = 一个子目录，含 `SKILL.md`。实测 frontmatter 结构如下（**字段不统一，解析要容错**）：

```yaml
---
name: dbskill                         # 必填，技能标识
description: |                       # 必填，可能单行或 | 多行块
  商业诊断工具箱，内置 24 个联动工具…
version: 2.15.1                      # 可选
type: skill                          # 可选
slug: dbskill-dontbesilent           # 可选
displayName: dbskill 商业诊断工具箱   # 可选（展示用）
category: business-diagnostics       # 可选（关系聚类可用）
platforms:                           # 可选
  - workbuddy
  - skillhub
---
# Markdown 正文…
```

**多文件 Skill 目录结构样例（dbskill）**：
```
dbskill/
├── SKILL.md
├── _skillhub_meta.json
├── config/
├── references/
└── scripts/
```

**关键事实（影响"关系"功能设计）**：
- 实测 grep 全部 SKILL.md 的 frontmatter，**几乎不存在** `depends` / `uses` / `related` / `calls` 这类关系声明字段。
- 仅个别 Skill 有 `category` / `platforms`。
- → **不能编造依赖图**。关系展示只能基于：分类字段（弱）、AI 聚类、用户手动连线。

---

## 4. 功能范围

### 4.1 必须做（P0）

1. **添加技能文件夹**
   - 「🧩 技能库」页签内提供「➕ 添加文件夹」按钮
   - 复用 Seegent 现有的文件夹选择/绑定机制（参考看板绑定文件夹 `bindFolderToBoard` 的交互与存储）
   - 支持添加**多个**文件夹，列表管理，可删除某个来源
   - 支持「重新扫描」刷新索引

2. **递归扫描与解析**
   - 对添加的每一个文件夹，递归查找所有 `SKILL.md`（每个含 SKILL.md 的父目录 = 1 个 Skill）
   - 解析 YAML frontmatter（name / description / version / category / platforms / slug / displayName 等，全部可选容错）
   - 统计：文件总数、子目录列表
   - 生成索引文件 `.seegent-skill-index.json`（结构见第 6 节）

3. **结构化浏览（三区布局）**
   - **左栏 · 分组/搜索**：按 `category` 分组（无 category 的归「未分类」）、按目录来源分组、按字母；顶部搜索框（匹配 name + description）
   - **中栏 · 卡片墙**：每个 Skill 一张卡片，显示
     - 展示名（displayName 优先，否则 name）
     - description 摘要（截断 ~120 字，可展开）
     - 标签：`category` / `platforms`
     - 多文件标记（📁，含子目录时）
     - 来源文件夹名
   - **右栏/下栏 · 详情**：点击卡片展开
     - 元信息区：name / version / category / platforms / slug
     - SKILL.md 全文：**Markdown 渲染**（复用现有 Markdown 渲染能力）
     - 文件树：列出该 Skill 目录下的文件与子目录（config/references/scripts…），点击子文件可在详情区查看其文本内容

4. **空状态与错误态**
   - 未添加任何文件夹：引导「➕ 添加你的 Skills 文件夹」
   - 文件夹内无 SKILL.md：提示「该文件夹未发现 Skill」
   - frontmatter 解析失败：Skill 仍展示，标注「元数据解析失败」，至少显示文件名

### 4.2 增强（P1，时间充裕再做）

5. **关系视图**
   - 短期：按 `category` / `platforms` / 来源目录 分组着色（弱关系，真实数据）
   - 中期：**AI 聚类**——调用 Seegent 现有 AI 对话接口，读取所有 Skill 的 description，自动打主题簇（如「数据库类 / 内容创作类 / 视频类」），在卡片上以主题色/簇标签呈现
   - 长期：用户在界面手动连线建立关系（拖拽）+ 预留解析 frontmatter 未来的 `depends` 字段
   - ❌ **不做**：凭空生成「依赖图」（真实数据无依赖声明）

6. **卡片上的「用 AI 解释这个 Skill」按钮**：调用现有对话接口，基于该 SKILL.md 内容生成一句大白话总结

### 4.3 Non-goals（明确不做）

- ❌ 不编辑 / 创建 / 删除 Skill 文件（只读浏览）
- ❌ 不执行 Skill
- ❌ 不自动抓取 connector 级 / 内置级 Skill（只加载用户手动添加的文件夹）
- ❌ 不做 Skill 市场 / 分享

---

## 5. 交互流程

### 5.1 添加技能文件夹
```
用户点「➕ 添加文件夹」
  → 系统文件夹选择器（复用现有绑定机制）
  → 用户选择 ~/.workbuddy/skills
  → 前端/后端递归扫描所有 SKILL.md
  → 解析 frontmatter → 写入 .seegent-skill-index.json
  → 卡片墙立即渲染
```

### 5.2 浏览与查看
```
技能库页默认 = 左分组 + 中卡片墙
  → 点卡片 → 右栏渲染 SKILL.md 全文 + 文件树
  → 点文件树子文件 → 右栏切换显示该文件文本
  → 搜索框实时过滤卡片
```

### 5.3 关系视图（P1）
```
点「关系」切换 → 按 category/主题簇 分组色块 或 力导向图（节点=Skill）
```

---

## 6. 数据模型

### 6.1 索引文件 `.seegent-skill-index.json`
```json
{
  "version": 1,
  "sources": [
    {
      "id": "src_1",
      "path": "/Users/shiyuanchang/.workbuddy/skills",
      "addedAt": 1783500000000,
      "skillCount": 4
    }
  ],
  "skills": [
    {
      "id": "skill_dbskill",
      "sourceId": "src_1",
      "dirName": "dbskill",
      "dirPath": "/Users/shiyuanchang/.workbuddy/skills/dbskill",
      "name": "dbskill",
      "displayName": "dbskill 商业诊断工具箱",
      "description": "商业诊断工具箱，内置 24 个联动工具…",
      "version": "2.15.1",
      "category": "business-diagnostics",
      "platforms": ["workbuddy", "skillhub"],
      "slug": "dbskill-dontbesilent",
      "fileCount": 12,
      "subDirs": ["config", "references", "scripts"],
      "files": [
        {"name": "SKILL.md", "path": "…/dbskill/SKILL.md", "size": 1234},
        {"name": "_skillhub_meta.json", "path": "…", "size": 200},
        {"name": "config/", "path": "…", "isDir": true}
      ],
      "parseError": false
    }
  ]
}
```

> 索引可存本地（项目目录 `.seegent-skill-index.json` 或 IndexedDB），不强制后端。

---

## 7. 技术实现建议

### 7.1 前端（见 `seegent/static/index.html`）
- 新增模块 `skill-library`（与现有 `file` / `chat` / `board` 模块并列，作为独立页签）
- 注册到 `switchModule()` 与侧边栏导航
- 复用现有能力：
  - 文件夹选择/绑定交互（参考 `bindFolderToBoard`）
  - Markdown 渲染（聊天/文档已用）
  - 搜索过滤组件
- frontmatter 解析：可用轻量 YAML 解析（或简单正则处理 `key: value` 与 `key: | 多行块`）；需容错字段缺失与多行块
- 递归读取：通过现有文件夹 handle / 后端 API 读取目录树（取决于现有架构，由执行 AI 按现有代码定）

### 7.2 后端（见 `seegent/server.py`，按需）
- 若前端无法跨目录读取（File System Access API 限制），后端新增：
  - `POST /api/skills/scan`：入参 `{ path }`，递归找 SKILL.md，解析 frontmatter，返回 skill 列表
  - `GET /api/skills/index`：返回 `.seegent-skill-index.json`
  - `GET /api/skills/file?path=...`：返回某个子文件文本内容（用于详情区查看 config/scripts 等）
- **优先复用现有文件读取能力**，不要重复造轮子

### 7.3 关键代码位置（供执行 AI 定位，行号可能漂移，以实际为准）
- 模块注册：`switchModule()` 附近（参考看板模块约 line 6954）
- 看板绑定文件夹参考：`bindFolderToBoard`（约 line 9201）
- 现有模块 HTML 模板：参考 `#board-module`（约 line 2904）
- 看板 CSS 参考：`/* ===== 业务数据看板 ===== */`（约 line 2016）

---

## 8. 验收标准

- [ ] 点击「➕ 添加文件夹」可选择本地目录，成功添加
- [ ] 可添加多个文件夹，可在列表中删除某个来源
- [ ] 添加后卡片墙自动出现该文件夹下所有 Skill（每个含 SKILL.md 的目录 = 1 张卡）
- [ ] 每张卡片显示展示名、description 摘要、category/platforms 标签、是否多文件、来源
- [ ] 点击卡片，右栏渲染该 SKILL.md 的 Markdown 全文
- [ ] 文件树可展开，点击子文件（如 config/、scripts/ 下文件）可查看其文本
- [ ] 搜索框按 name + description 实时过滤
- [ ] 左栏可按 category / 来源 / 字母分组
- [ ] 空状态（无文件夹 / 无 SKILL.md / 解析失败）均有明确提示
- [ ] 刷新页面后技能库索引仍在（持久化）
- [ ] 现有功能（文件管理、AI 对话、看板、模型/角色/MCP）不受影响

---

## 9. 约束与注意事项

- **隐私优先**：所有解析在本地完成，Skill 内容不上传任何第三方
- **容错**：frontmatter 字段缺失 / 格式不规范时不能崩，至少展示文件名
- **不破坏现有架构**：新增独立模块，不动 file/chat/board 等现有模块逻辑
- **复用优先**：文件夹选择、Markdown 渲染、搜索、文件读取均复用现有实现
- **description 可能极长**（如 web-video-presentation 的 description 近 300 字）：卡片摘要必须截断，详情区才显示全文

---

## 10. 与看板改造的关系

本方案与 `看板升级-首页改造-实现方案.md` 是**两个独立模块**：
- 看板 = 项目数据 / 待办 / 最近活动（指挥部视角）
- 技能库 = 本地 Skill 资产浏览（能力资产视角）

两者并列存在于 Seegent，互不依赖。可分别交给不同 AI 实现，也可同一 AI 顺序实现。
