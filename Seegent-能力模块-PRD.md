# Seegent 能力模块方案（团队 + 技能 + 配置 + 扩展）

> 版本：v0.4 | 日期：2026-07-14
> 性质：模块结构定案 + 个人/项目资产分离 + **实现落地记录**，承接 2026-07-08 定位（A 指挥部，拒绝 B 多智能体）
> 取代：Seegent-能力库-PRD.md（v0.1）、Seegent-能力模块-PRD.md（v0.2 用"助手"命名 / v0.3 模块定案）
> 本次变更（v0.4）：补齐 **T8 配置瘦身** 与 **T9 角色源改接 syteam** 的实际落地；修正 v0.3 中"扩展=占位"与代码现状不符之处（扩展实为 MCP 配置，保留为配置页 subtab）
> 关联：Seegent-PRD.md、Seegent-定位与功能梳理.md、看板升级-首页改造-实现方案.md

---

## 0. 一句话定位

**Seegent 不做执行，只做"能力资产"的管理与出口。** 你的团队（角色）和技能，是你自己的、可携带的 AI 资产；存放在个人根目录 `拾元/` 下的 `syteam/`、`syskill/` 文件夹里，Seegent 负责把它们呈现好、随时导出去别处用。执行永远在别的平台。`Seegent/` 项目目录本身不含任何个人信息。

---

## 1. 模块结构总览（本次定案）

```
Seegent（应用，项目目录，无个人信息）
├── 看板      （已有）数据呈现 + 信号，不变
├── 团队      （新）个人资产 拾元/syteam  → 角色中台，组织架构式呈现 + 召唤
├── 技能      （新）个人资产 拾元/syskill  → 技能中台，分组呈现 + 召唤
├── 配置      （改）保留「模型」+「扩展(MCP)」，其余（助手/技能配置）移除
└── 连接器    （移除）不做，详见 §7
```

| 模块 | 数据位置 | 本期状态 | 一句话 |
|------|---------|---------|--------|
| 看板 | 项目内 | 维持 | 数据陈列 → 信号（另立项） |
| 团队 | 个人 `拾元/syteam` | **做** | 你的虚拟团队，组织架构式管理 + 召唤 |
| 技能 | 个人 `拾元/syskill` | **做** | 你的方法库，分组管理 + 召唤 |
| 配置 | 项目内 | **模型 + 扩展(MCP)** | 模型选择 + MCP 扩展配置；其余移除 |
| 连接器 | — | **移除** | 不调用、不编排任何外部 Agent |

> 注：此前 v0.2 曾用"助手"命名，本版按你的要求**改回"团队"**。文件夹实际命名为 `syteam`（= 拾元 team）与 `syskill`（= 拾元 skill），模块显示名分别为"团队""技能"。

---

## 2. 关键原则：个人资产与项目文件分离

这是本次最重要的架构决定：

- **`拾元/` = 个人根目录**，代表"完全属于我个人的内容和文件"。内含：
  - `syteam/` —— 团队（角色库）
  - `syskill/` —— 技能库
  - 以及你其他个人文件（与 Seegent 无关的也放这儿）
- **`Seegent/` = 纯项目目录**，只放项目相关的代码、配置、文档，**不含任何个人信息**。
- Seegent 应用运行时从 `拾元/` 读取团队 / 技能资产，但项目目录本身保持干净、可独立提交 / 分享 / 开源。

这样分离的好处：
1. **隐私**：项目目录可公开 / 交给协作方，绝不会带上你的个人角色与技能。
2. **可携带**：`拾元/` 是你的个人知识底座，整体复制 / 迁移即可带走全部 AI 资产。
3. **清晰**：个人归个人，项目归项目，互不污染。

### Seegent 如何定位 `拾元/`（待定，推荐默认约定）

- **推荐默认约定**：个人根固定在用户主目录下的 `拾元/`（即 `~/拾元`）。Seegent 启动即扫描 `~/拾元/syteam` 与 `~/拾元/syskill`。
- **若需改位置**：通过首次运行的「个人根目录」一次性设置（不属于"配置"模块，不进配置页），或环境变量 `SEEGENT_PERSONAL_ROOT`。
- **理由**：配置模块只保留模型（§6），故个人根位置不占用配置页，用"约定 + 一次性设置"解决，不破坏极简配置原则。

---

## 3. 背景与动机（Why）

用户是同时运营多个项目（识字 App、Learn Chinese、小红书等）的独立开发者。真实瓶颈不是"没有 AI 帮忙"，而是：

- 角色和技能散落在各平台对话里，想复用时找不到、复制不全；
- 已有的 12 个内置角色是死配置，和真实工作流脱节，几乎不用；
- 每次换平台（Claude Code / WorkBuddy / Cursor / ChatGPT 网页）都要重写一遍"你是谁、用什么方法"；
- 想要的是**随时可调用的"我的能力库"**：按当下内容需求，随手召唤对应团队角色 + 对应技能，到任何正在用的智能体里。

一句话：**在 AI-native 工作流里，角色和技能是用户自己的可移植资产。Seegent 负责定义、管理、导出；执行在别处。**

---

## 4. 核心概念：团队角色 vs 技能，以及如何组合

| 维度 | 团队角色（Member） | 技能（Skill） |
|------|-------------------|--------------|
| 回答的问题 | 以什么身份 / 视角来做 | 用什么方法 / 流程来做 |
| 例子 | 数据解读专家、内容主编、项目经理 | 小红书爆款标题、竞品分析框架、周报生成 |
| 文件位置 | `拾元/syteam/` 文件夹 | `拾元/syskill/` 文件夹 |
| 在召唤里的作用 | 定"人设 / 口吻 / 专业立场" | 定"操作步骤 / 模板 / 约束" |

组合召唤（用户描述的"画面感"）：

```
【团队角色】请以"数据解读专家"的身份（设定见 ./syteam/数据组/小辉.md）
【技能】按"趋势解读"的方法执行（见 ./syskill/数据组/趋势解读.md）
【任务】这是我这周的小红书数据：<粘贴数据>
请结合以上团队角色与技能，给出趋势判断和下周建议。
```

团队角色给"立场"，技能给"方法"，任务是"具体内容"。三者可拆可合——这正是 AI-native 可组合能力的核心。

---

## 5. 团队模块（文件夹驱动，个人资产）

### 5.1 文件夹约定

个人根 `拾元/` 下，约定专属文件夹 `syteam/`：

```
拾元/
└── syteam/                ← 团队（角色库，即你的虚拟组织架构）
    ├── 数据组/
    │   ├── 小辉.md         （角色：AI/代码知识库）
    │   └── 小禾.md
    ├── 内容组/
    │   └── 小蓝.md
    └── 管理组/
        └── 小琪.md
```

- 子文件夹 = 分组（方案 A，最贴近你那张组织架构图）。
- 每个 `.md` = 一个团队角色。
- 识别：扫描到 `拾元/syteam/` 即视为团队库，自动在"团队"页呈现。

### 5.2 文档格式

`拾元/syteam/数据组/小辉.md`：

```markdown
---
seegent-type: team
name: 小辉
title: AI/代码知识库
group: 数据组
emoji: 📚
datasources: [小红书运营数据]
---

（职责、专业立场、输出风格、注意事项……）
```

`seegent-type: team` 区分类型；`group` 用于分组；正文即召唤时粘贴的内容。

### 5.3 自动识别

- 应用启动 / 刷新时，扫描 `拾元/syteam/`。
- 命中即纳入，无需手动配置。
- 新增 / 改名 / 删除 `.md`，刷新即同步（单一事实源，无额外存储）。

### 5.4 页面（照你提供的组织架构图样式）

- 独立模块"团队"。
- 左侧分组列表（数据组 / 内容组 / 管理组…）→ 右侧该组成员。
- 每个成员一行：图标 + 名字 + 职责一句话 + 操作（查看文档 / 召唤）。
- 点成员 → 右侧面板渲染该 `.md` 完整正文（markdown）。

### 5.5 与对话模块的衔接（角色源改接 syteam）

> 落地状态（v0.4）：已实现。

- **不再依赖旧的内置 12 角色**（通用助手 / 文案写手 / 教程讲师 …）。这些角色与真实工作流脱节，已被移除（见 §10）。
- 对话模块的「角色」选择器数据来源改为后端动态合并，顺序如下：
  1. **兜底预设（2 个，保留）**：`通用助手`（无 system prompt，基础模型）、`自定义`（用户自行填写提示词）。
  2. **团队角色**：扫描 `拾元/syteam/` 下的每个 `.md`，自动转为可选项。映射关系：`content`（.md 全文）→ system prompt，`group`（子文件夹）→ 分类，`emoji` → 图标，并打 `isTeam` 标记。
  3. **用户自建角色**：用户在角色管理器里新建 / 编辑的自定义角色。
- **实现方式（关键）**：后端 `/api/roles` 的 GET 改为实时合并上述三类；旧的 `.seegent-roles.json` **不物理改写**，仅由接口层过滤掉内置 12 角色，文件在用户首次保存角色时自动清理为只含自建角色。（已备份 `.seegent-roles.json.bak-20260714`，可回退。）
- 团队角色在角色管理器里**不显示编辑 / 删除按钮**——它的真身是 `拾元/syteam/` 里的 .md 文件，应在文件里改，避免两边不一致。
- 不选手动角色 = 基础模型、无 system prompt（保持兜底行为）。

---

## 6. 技能模块（文件夹驱动，个人资产）

结构与"团队"平行，只是文件夹为 `拾元/syskill/`、文档 `seegent-type: skill`。

### 6.1 文件夹约定

```
拾元/
└── syskill/               ← 技能库
    ├── 内容组/
    │   └── 小红书爆款标题.md
    └── 数据组/
        └── 趋势解读.md
```

### 6.2 文档格式

`拾元/syskill/数据组/趋势解读.md`：

```markdown
---
seegent-type: skill
name: 趋势解读
group: 数据组
emoji: 📈
inputs: [时序数据]
outputs: [趋势判断, 建议]
---

（操作步骤、模板、约束、示例……）
```

### 6.3 页面

独立模块"技能"，布局同团队：左侧分组 → 成员行（图标+名+一句话+查看/召唤）→ 点开看正文。

### 6.4 通用召唤机制（团队与技能共用，本方案核心交付）

每个团队角色 / 技能详情处一个"召唤"按钮，选目标平台生成指令：

| 目标 | 生成内容 | 用法 |
|------|---------|------|
| 本地智能体（Claude Code / WorkBuddy / Cursor，能读 `拾元/`） | `请读取 ./syteam/数据组/小辉.md 并按其设定扮演，帮我：<任务>` | 复制 → 粘贴 |
| 通用平台（ChatGPT 网页等，读不到本地路径） | 把文档正文整段 + "请按以上设定…" | 复制 → 粘贴 |

- **组合召唤**：在团队页点"复制指令"、在技能页点"复制指令"，拼到一起发给目标平台（见 §4 示例）。
- 召唤是**纯文本**，不依赖 API / 连接器 / 任何技术打通。今天即可用。
- 深度"一键派单自动跑"属原 C 连接器方向，已确认**不做**（§7）。

---

## 7. 连接器（移除，不做）

- **决定**：彻底不做。原 PRD 的 C「未来轻连接 / 一键派单到 WorkBuddy 自动跑」方向划除。
- **理由**：与"Seegent 只管理、不执行"的定位冲突；且是重活（需 API、鉴权、状态管理）。用户明确"连接器这个也不做"。
- **影响**：召唤一律走纯文本复制（§6.4），不提供任何自动调用。这反而让产品今天就能用、零依赖。

---

## 8. 配置模块（模型 + 扩展 MCP）

> 落地状态（v0.4）：已实现，配置页仅剩「模型」「扩展」两个 subtab。

- **保留项一：模型选择。** Seegent 仍要保留最低限度的 AI 能力（如：看板信号的一句话解释、文档内问答、召唤指令的辅助生成），这些依赖一个可调的底层模型。
- **保留项二：扩展（MCP）。** 配置页保留「扩展」subtab，用于管理 MCP 服务器（如飞书、fetch 等），让 AI 在对话中可调用对应工具。这是真实的配置功能，非占位。
- **已移除的配置项**：原配置页的「助手」「技能」两个 subtab 已移除——助手(Agent) 配置本期不做；技能配置由新的顶层「技能」模块（个人 `syskill/`）承接，不再放在配置页里。
  - 说明：看板自身的数据源（如飞书多维表格）连接仍归属「看板」模块的配置，不属于这里；本「配置」模块只管全局模型 + MCP 扩展。
  - 说明：个人根目录 `拾元/` 的位置不属于配置页（见 §2），用约定 + 一次性设置解决。
- **原则**：配置页极简，避免回归"死配置堆砌"。未来若确有需要，再逐项加回。

---

## 9. 扩展（MCP 配置，已实现）

- **落点**：v0.4 中「扩展」不再是占位，而是配置页内的「扩展(MCP)」subtab，用于管理 MCP 服务器（飞书、fetch 等），让 AI 在对话里可调用对应工具。属真实可用的配置功能。
- **不做顶层占位导航**：经确认，「扩展」不提升为顶栏独立入口，维持配置页 subtab 形态，避免与「模型」割裂、也避免重复。
- **未来扩展形态**：若后续需要插件市场、外部能力接入等更丰富的扩展形态，再单独立项评估，不在本期定义。

---

## 10. 非目标（Non-goals）

- 不在 Seegent 内执行团队角色或技能（不写代码 / 做图 / 跑自动化）。
- 不做多 Agent 编排、DAG、Agent 注册表（B 路线，已否）。
- **不做任何连接器 / API 级调用**（§7）。
- 不在团队 / 技能页内做富文本编辑器（编辑跳文件模块）。
- **不内置旧的 12 个业务角色**（文案写手 / 教程讲师 / 知识编辑等）；仅保留「通用助手」「自定义」两个兜底预设，团队 / 技能角色全部由用户自己定义。
- **不在 `Seegent/` 项目目录内存放任何个人信息**（§2）。
- 扩展（MCP）配置属已有能力，本期保留（§9）；更丰富的扩展形态不在本期定义。

---

## 11. 成功指标

| 指标 | 定义 | 目标（基线待定） |
|------|------|----------------|
| 召唤采用率 | 每周从团队/技能发起"召唤"的次数 | > 0，逐步上升 |
| 资产复用率 | 单个角色/技能被召唤的频次 | 核心角色周复用 ≥ 3 次 |
| 内置角色零回退 | 对话是否仍依赖旧内置角色 | 0 依赖 |
| 管理效率 | 新增一个角色/技能的平均耗时 | < 5 分钟（写一个 .md） |

---

## 12. 路线图

> 状态（v0.4）：Now 已全部落地，详见文末「实现记录」。

- **Now（P0，已完成）**：文件夹约定（`拾元/syteam`、`拾元/syskill`）+ 文档格式 + 自动识别 + 团队页（组织架构式呈现）+ 技能页（分组呈现）+ 通用召唤机制（本地路径 / 通用平台正文）+ 角色源改接 `拾元/syteam` + 配置瘦身（模型 + 扩展 MCP）+ 个人/项目分离落地 + 移除内置 12 角色。
- **Next（P1）**：组合召唤辅助（一键拼「团队角色 + 技能」指令，减少手动复制拼接）；团队 / 技能空态引导文案打磨。
- **Later（P2）**：与看板首页打通（首页显示"我的团队 / 能力"概览）；评估与现有「技能库浏览器」的合并 / 取舍；更丰富的扩展形态定义（若届时确有需求）。

---

## 13. 实现记录（v0.4 落地摘要）

| 项 | 前端（index.html） | 后端（server.py） | 验证 |
|----|-------------------|------------------|------|
| 团队页 | 新增「团队」导航 + `initTeam` 三栏模块 | 新增 `/api/team`（扫 `拾元/syteam`，frontmatter 可选，子文件夹=分组） | 端到端实测通过 |
| 技能页（个人专属） | 新增「技能」导航（区别于本机通用「技能库」）+ `initSkill` 三栏模块；模块内注明"个人专属 vs 本机通用" | 新增 `/api/personal-skills`（复用 `_scan_skill_source`，返回 6 项：1 目录型 + 5 文件型） | 实测返回 6，前端 JS 过 `node --check` |
| 通用召唤 | 团队 / 技能详情均有「召唤」按钮，生成本地路径指令或贴正文 | — | 弹窗复制可用 |
| 配置瘦身 | 配置页仅「模型」「扩展(MCP)」两 subtab；移除「助手」「技能」subtab 及对应面板；`switchConfigTab` 放开对 mcp 的限制 | — | 实测两 tab + 两面板均在 |
| 角色源改接 syteam | 角色管理器对团队角色隐藏编辑 / 删除按钮 | `/api/roles` GET 改为动态合并（兜底预设 2 + syteam 角色 + 用户自建），过滤旧内置 12 角色；备份 `.seegent-roles.json.bak-20260714` | 往 syteam 丢 .md 即时出现为聊天角色，删之即退 |
| 个人/项目分离 | 团队 / 技能数据全在 `~/拾元`，`Seegent/` 不落个人信息 | `SEEGENT_PERSONAL_ROOT` 环境变量可改个人根 | 架构保证 |

> 注：v0.3 曾把「扩展」写成"占位、本期不实现"，与代码实际（扩展 = MCP 配置 subtab）不符；v0.4 已据实修正（§9）。因 T8 配置瘦身时一度误删「扩展」subtab，已还原。

---

## 14. 风险与开放问题

- **资产维护**：库的价值取决于文档质量。需引导写好 frontmatter + 正文（给模板）。
- **个人根位置**：默认 `~/拾元` 是否合你意？若 `拾元/` 在别处，需确认一次性设置方式（§2）。
- **与现有技能库浏览器的关系**：项目已有 `技能库浏览器`，定位可能重叠，需明确分工或合并（P2 评估）。注意该浏览器属项目内功能，与个人 `syskill/` 是两回事。
- **识别误判**：若 `拾元/` 下恰有同名目录，可能误识别。可用根目录标记文件 `.seegent-capabilities` 兜底。
- **多项目**：每个项目根可有自己的 团队/技能，还是全局一套？本版定为全局个人根 `拾元/`，所有项目共享同一套团队与技能（符合"个人资产"定位）；后续若需项目级覆盖再议。

---

*状态：v0.4 已落地（团队 / 技能 / 配置瘦身 / 角色源改接 syteam 均实现并验证）。下一步：P1 组合召唤辅助、空态文案打磨。*
