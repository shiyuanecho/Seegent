---
status: approved
source_date: 2026-09-01
owner: shared
depends_on:
  - CLAUDE.md
open_questions: []
---

# 趋势模块 v1 规格

## 目标

在 Seegent 中增加一个面向单一、本地桌面用户的「趋势」模块，让用户能够按中文名、英文名、Symbol 或合约地址找到资产，选择具体 CEX 市场或 DEX 交易池，查看来源明确的行情、K 线、交易活动与数据覆盖状态，并把收藏对象保存在本机。

## 已确认范围

- 模块内采用与 Agent 页面一致的二级页面切换：`市场总览 / 我的收藏 / 代币详情`。
- 页面路径：市场总览 → 搜索 → 收藏或选择资产与市场 → 代币详情；收藏卡也可直接进入详情。
- 链：Robinhood Chain、Ethereum、BSC、Base、Solana、TON、Morph。
- 核心免 Key 来源：Binance、GeckoTerminal、DEX Screener、DefiLlama、Robinhood Stock Token。
- 可选增强来源通过环境变量启用；密钥不进入前端、localStorage 或仓库。
- 后端时间戳统一 UTC，前端显示本地时间并标注时区。
- 页面打开时刷新；切走、页面隐藏或关闭后停止。v1 不做后台常驻、系统通知或交易执行。

## 数据协议

- `Asset`：原生币或合约代币；BTC 等原生资产允许没有合约地址。
- `Market`：CEX 交易对、DEX Pool 或 Robinhood Stock Token 报价市场。
- K 线绑定 `marketId`；风险和转账绑定 `assetId`。
- 所有指标携带来源、抓取时间、覆盖时间和 `exact / estimated / partial / unavailable` 状态。
- DEX 买卖方向以当前选中资产为视角；来源只给买卖笔数时不推导买卖金额。
- Token Transfer 的代币数量和美元价值分开；没有历史估值时不按当前价格伪造历史美元金额。

## 时间与刷新

- 资金活动/逐笔成交窗口：1m、5m、15m、30m、1h、4h、12h、1d、7d、30d；它只影响活动统计，不改变 K 线历史覆盖。
- K 线周期：1m、5m、15m、30m、1h、4h、12h、1d、7d、1M。详情页只保留一个“周期（每根）”选择器；选择 15m 就是一根 15 分钟 K 线，选择 1M 就是一根月 K 线。
- K 线覆盖统一为 `all`。Provider 使用最多 1000 根一页的历史游标分页；前端打开详情先读取并立即展示最新一页，历史按“加载更早历史”逐页读取，过细周期或限流时保留已读数据，不偷偷改成更粗周期。
- 返回 `requestedInterval`、`effectiveInterval`、`providerInterval`、实际覆盖时间、`nextCursor`、`historyComplete` 和 `nativeOrAggregated`。`effectiveInterval` 必须等于用户所选周期；只有 Provider 没有稳定的 7d/1M 原生周期时，才用真实日线按 UTC 聚合。
- 图表时间轴和自定义悬停提示只保留一个时间标签，隐藏 Lightweight Charts 的重复交叉线日期标签；缺失周期不补假 K 线，长历史通过缩放与拖动查看。
- CEX 行情约 10 秒、DEX 摘要约 20 秒、活动约 30 秒、K 线约 60 秒、链概览约 5 分钟。
- Provider 使用独立缓存、速率预算、超时和陈旧数据回退。

## 页面状态

- 首次加载、正常、无结果、同名多市场、部分成功、数据陈旧、限流、网络断开、Key 缺失、链不支持和无数据。
- 缺失值显示「暂不可用」或具体原因，不显示为 0。
- 搜索结果始终展示链、完整合约缩写、市场、24h 成交额、市值、流动性、市场创建时间、来源和风险状态；同名代币不自动合并。
- 搜索结果支持按综合相关性、24h 成交额、市值、流动性和市场创建时间排序，并支持升序/降序。综合相关性保留后端 Provider 的原始顺序。
- 搜索结果支持设置 24h 成交额下限、市值下限和市场创建时间范围；筛选与排序只作用于本次最多 30 条返回结果，不暗示全市场排名。
- `market.createdAt` 表示公开数据源提供的交易池/市场创建时间，不等同于 Token 合约部署时间；界面统一显示为「市场创建」。
- 代币图标只接受 Provider 返回的 HTTPS `logoUrl`/`imageUrl`；没有可用图标或图片加载失败时不渲染头像，也不使用 symbol 首字母伪造占位图。
- 当排序字段缺失时，该结果始终排在有数据结果之后；启用对应阈值筛选时，字段缺失的结果不满足筛选条件。不能把缺失值按 0 处理。
- 搜索筛选偏好只保存在浏览器 localStorage；支持一键重置，不保存查询内容或任何密钥。
- “我的收藏”始终是可直接进入的独立页面；收藏卡显示最近价格快照、24h 涨跌、24h 成交量、链、市场、来源与更新时间。
- 搜索结果和代币详情都可以添加或取消收藏；收藏保存在本机，重新启动与刷新页面后恢复。
- 收藏页打开时只刷新当前可见收藏的行情，离开收藏页后停止该轮询，避免收藏数量增加后无上限请求数据源。

## 明确不做

- 交易所个人账户充值、提现、持仓和订单。
- 钱包签名、转账、兑换、授权或交易执行。
- CryptoQuant、Nansen、Arkham 等实体地址资金流。
- 页面关闭后的后台监控和系统级提醒。
- 对所有链承诺同等级数据；能力由运行时 Provider 状态决定。

## v1 验收

- 不配置密钥也能打开趋势模块、看到七条链、搜索 BTC/比特币和合约地址。
- 能明确区分资产与市场，展示至少一个真实行情/K 线来源或清楚的不可用原因。
- 时间范围切换、收藏列表保存、来源状态、部分失败和离开页面停止刷新均可验证。
- 市场总览、我的收藏和代币详情可独立切换；收藏支持搜索结果直接添加、删除、刷新恢复和点击回到详情。
- 同名结果较多时可以按 24h 成交额、市值、流动性或市场创建时间排序，设置成交额/市值/时间筛选，并在刷新后恢复筛选偏好。
- 现有项目、封面、看板、用量、Agent 和设置模块无回归。
